import express from 'express';

const router = express.Router();

// TODO: Move S&OP-related endpoints from routes.js to here
// This includes:
// - GET /sop-cycle-configs
// - POST /sop-cycle-configs
// - POST /sop-cycle-configs/:configId/generate
// - GET /sop-cycles
// - POST /sop-cycles
// - PUT /sop-cycles/:cycleId/status
// - GET /sop-cycles/:cycleId/permissions
// - POST /sop-cycles/:cycleId/permissions

export default router; 