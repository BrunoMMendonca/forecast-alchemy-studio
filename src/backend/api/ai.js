import express from 'express';

const router = express.Router();

// TODO: Move AI-related endpoints from routes.js to here
// This includes:
// - POST /grok-transform
// - POST /grok-generate-config
// - POST /apply-config
// - POST /ai-optimize
// - POST /ai-model-recommendation
// - POST /process-ai-import

export default router; 